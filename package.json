{
  "name": "bwn-visualizer",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && mkdir -p dist && node node_modules/pug-cli/index.js views/index.pug views/soundscape.pug views/victory.pug -P -o dist && node node_modules/cpy-cli/cli.js \"public/**\" dist --parents && node scripts/fix-routes.js"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "pug": "^3.0.3",
    "pug-cli": "^1.0.0-alpha6",
    "rimraf": "^5.0.0"
  }
}
