{
  "name": "bwn-visualizer",
  "private": false,
  "scripts": {
    "dev": "node app.js",
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && mkdir -p dist && node node_modules/pug-cli/index.js views/index.pug views/soundscape.pug views/victory.pug views/singularity.pug -P -o dist && cp -a public/. dist/ && node scripts/fix-routes.js"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "pug": "^3.0.3",
    "pug-cli": "^1.0.0-alpha6",
    "rimraf": "^5.0.0"
  },
  "version": "1.0.2",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "http-errors": "^2.0.0",
    "morgan": "^1.10.1"
  }
}
